# Program prints the maximum consistency to the console. 

# Quick Start

```
open index.html with web-browser;
array generates automatically;
open console in browser`s devtool and look for result.

```
